# 依賴金絲雀(批#24):每週一以「requirements.txt 最新允許版本」跑完整測試——
# production 全面鎖版(requirements.lock)後,新版相容性由本 job 提前發現;
# 紅燈=該升級 lock 前先修相容性。與 production 完全隔離,失敗不影響晨報。
name: deps-canary
on:
  schedule:
    - cron: "0 21 * * 0"   # 週一 05:00 台北
  workflow_dispatch:
permissions:
  contents: read
concurrency:
  group: deps-canary
  cancel-in-progress: true
jobs:
  canary:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5  # v4(與其他 workflow 同 pin)
      - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065
        with:
          python-version: "3.11"
      - name: Install latest allowed deps
        run: |
          # 批#35 r2(Codex):**不**升級 pip——未鎖版 pip 會被下載並立即執行,
          # 而本 job 持有寫入權限/金鑰;runner 內建 pip 已足以做 --require-hashes 安裝。
          pip install -r requirements.txt
          # 測試工具不屬於「最新允許的執行期依賴」這個受測對象,但沒有它
          # 就跑不了測試(外審 2026-09-04 P3 拆檔之後 requirements.txt 裡
          # 不再有 pytest)。這裡刻意不鎖版:金絲雀本來就是要撞新版本。
          pip install pytest PyYAML
          pip list
      - name: Compile + tests
        run: |
          python -m compileall -q .
          python -m pytest tests/ -q
