# 晨報品質改善：取材、縱向分析與手機閱讀

## 查明的問題

- 9/4、9/5 存檔信缺少「九、其他類股資訊」。9/5 舊班次的 manifest
  記錄科技 9 則、其他 9 則皆完成 Markdown 渲染，但最終 HTML 沒有第九段。
  存檔約 86 KB；這次缺段不是 Gmail 截信造成，而是舊版 6,000 字保險絲。
  基底版本已將保險絲提高至 40,000 字，本次沒有再次調高。
- 9/5 有 571 則取材，模型新聞清單上限為 220；原本除了必分析事件代表，
  其餘按品質／時間排序，類股素材可能被熱門科技新聞排擠。
- 該班 6 則高重要性分析，4 則有營運到財務的傳導；6 則皆標記單一來源。
  文章多不等於獨立佐證多，不把轉載數量當作證據強度。
- 既有 schema 有量級理由、時窗與確認條件，但新聞散文沒有呈現這些欄位。

## 本次調整

1. 修正地緣 Google News 查詢的 AND 過度限縮，並補半導體設備、能源電網取材。
   2026-09-06 實際 RSS HTTP 驗證皆為 200：舊地緣 0 則、OR 版 100 則，
   設備 13 則、能源電網 32 則。這是取材端回傳數，不是最終採用或有效獨立事件數；
   日期、內容匹配、去重與各 feed 項目上限仍適用。
2. 在原有 220 則預算內，按類股／世界取材桶輪流保留最多 3 則；必分析事件代表
   優先且不被排擠。去重換代表時保留有限白名單內的取材標籤。不從內文猜公司、
   不提高計分權重、不把分類標籤當作獨立來源。缺料不補造。
3. 主分析與 legacy 備援共用縱向寫作規則：新增事實 → 營運變數 → 財務影響，
   分開市場反應與財報實現時點；與昨日觀點比較，明示證據缺口、確認及推翻條件。
   既有量級／觀察窗／確認條件融入同一小段散文，不改回多層清單，不追加模型呼叫。
4. 最終 HTML 另記 `llm.email_html`，核對第八／九／十段與新聞傳導卡數。
   若內容遺失或檢查／手機增強失敗，保留原信、發 warning，品質判準報 defect。
   舊 manifest 沒有此診斷不被回溯誤判；legacy 無傳導標記時只驗段落存在，
   不宣稱已逐卡核對。
5. 手機正文 16 px、寬表格改直向欄位；桌面保留原表格。只轉換有完整欄名、
   無合併儲存格的矩形資料表，不動巢狀排版表、不刪區塊、不前移 Top5。
   平日與週日信均套用；極簡備援保留簡單 inline HTML。漸層卡片補純色背景。

手機 CSS 僅依賴 class、width media query、display 等 Gmail 文件列出的功能，
不使用 JavaScript 或暗色反轉技巧：[Google Gmail CSS 支援](https://developers.google.com/workspace/gmail/design/css)。

## 驗證及限制

- 改前完整基準：3,527 passed、2 skipped。
- 第一批修正後針對性測試：57 passed，包含真的正規化管線、去重、必分析優先、
  9+9 與 12+10 生產量級分析到最終 HTML、手機表格與錯誤降級。
- 離線呼叫實際 renderer，12 則科技與 10 則其他類股均保留，無段落遺失。
  使用合成新聞測試資料，不寄信、不呼叫 LLM、不改生產 state。
- 另以 9/5 存檔做「只換排版」預覽，在 375、390、430 與 900 px 視窗檢查：
  無橫向溢出；存檔中的 4 張寬資料表可轉成手機直向欄位。已目視手機與桌面截圖。
  此存檔本身缺第九段，排版預覽不會憑空復原舊信的分析。
- 本機預覽在忽略的 `artifacts/quality-0906/`，不發佈測試或存檔信件內容。
- 瀏覽器模擬不是 iPhone Gmail 真機、暗色模式或所有帳戶種類的保證。
  長信仍可能被 Gmail 折疊；依使用者既有要求保留內容，不為縮短信件刪段。
- 這些離線驗證不能證明下一次真實模型輸出的因果分析必然更好；需要後續班次的
  `primary_metrics` 與 `email_html` 再觀察。沒有改模型、推理強度、API 預算、
  預測係數、持股處理或排程寄送方式。

完整 CI 與模型 diff review 依 AGENTS.md 閘門執行；未完成或 quota pending 不等於通過。

## 獨立審查第一輪的兩項修正

- P2，CONFIRMED：中彰投實際來源為「房市-中彰投／建商-中彰投／建設-中彰投」，
  不是合成測試的「中彰投建設」。已加入明確映射，並用全部真實類股註冊表名稱
  測試分類與保留席次，避免測試只證明一個不存在的來源。
- P2，CONFIRMED：trim 先量舊 HTML，再加手機欄名可能重新超標。離線重現
  106.8 KB 且沒有裁去可選資料表。已將手機增強移入每次組裝，讓既有 keep／trim
  尺寸決策直接使用實際郵件；最終診斷仍對應最後回傳的 HTML。
  不更改預設模式、裁減順序或原有內容取捨政策。
- 第一輪完整本機 pytest：3,554 passed、2 skipped；修正後需重新完整驗證及複審。
