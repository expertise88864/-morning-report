# 晨報優化驗收清單（2026-09-23 基準）

本表只記錄可核對的工程與正式排程證據；不含持倉、個人配置或信件正文。`a9380d7f9712c622d479f6f8f87e70e2a5f00ed0` 已正式發佈，但其精確 commit 審查仍待 Claude 額度恢復補審。本表的未發佈變更不能算作已改善讀者收到的信。

| ID | 基準證據與讀者影響 | 根因／待確認 | 修正與離線驗證 | 發佈 SHA／正常信驗收 |
| --- | --- | --- | --- | --- |
| E1 信件完整性 | 9/23 `state/run_manifest.json` 的最終 HTML 119,371 bytes；`gmail_clipping_possible=true`，內容稽核未報遺失段落。9/17–9/23 七封封存信有五封超過約 102 KiB，Gmail 收件端仍可能截斷。 | `EMAIL_OVERFLOW_MODE=full` 保留全部內容；9/23 當時品質告警尚未消費完成渲染後的風險旗標。9/23 HTML 的 inline style 屬性值約 37,685 bytes、連結 href 值約 9,343 bytes；不能只靠刪空白達到 95 KiB。 | 已上線的 `email_quality_findings.assess()` 對超過 102 KiB 的最終 HTML 標記 `email_gmail_clipping_risk`，但只揭露風險、不防止截斷。先前移除手機表格中無作用的重複 `mail-cell` 類別；舊版離線樣本從 69,691 降為 68,731 bytes，連結與行列回歸通過；9/23 實際信沒有 `mail-cell`，不能把該樣本的縮減算成正式信改善。**仍未壓至 95 KiB**；須在不刪必要內容、連結或手機樣式的前提下，另驗證主要體積來源及收件端效果。 | 告警已於 `511de58885962f1197a213174150d85b8fcfdca3` 上線，並由 `dac63a1b41b474cad29034244efd7af1d8fa0e0b` 結案精確審查；本輪壓縮器修正待發佈。兩者均待正常信與收件端檢查。 |
| W1 到期觀察點 | 9/23 manifest 記錄 `watch_expired_unreviewed=1`；9/22 state 的 `w49` 建立於 9/21、期限 9/22、`last_reviewed` 空白。讀者少一次期限內回顧。 | 唯讀重放 9/22 state：開啟 7 條、容量 8 條，`usable_watch(..., '2026-09-22')` 的 7 個結果包含 `w49`，故非排序／容量漏選。9/22 manifest 為 `legacy_fallback_after_luna_failure`，備援信未完成該條驗證回顧；9/23 已過期限，不能事後改寫或假稱已回顧。 | 已上線的 `fallback_watch_gap` 會在備援當天揭露到期觀察點未完成驗證，於信中用自然語句提醒，品質告警附非敏感 ID／期限；不改帳本狀態、不回填。這讓缺口不再延後一天才浮出，**仍未使備援具備真正的逐項證據回顧**。 | `511de58885962f1197a213174150d85b8fcfdca3` 已上線、`dac63a1b41b474cad29034244efd7af1d8fa0e0b` 已結案精確審查；待正常信驗收，不得回填正式 state。 |
| A1 特化與成本 | 9/23 正式執行記錄 `analysis_origin=luna_specialized`、兩次呼叫、估計總成本 USD 0.141116；此版本的主分析與成本尚未經 `a9380d7` 後的正常信驗收。 | 舊版 9/21–9/22 曾分別遇 402／`max_output_tokens`；不可用一次成功宣稱連續可靠。 | 以既有失敗快照測試輸入切片、修補與備援，不做付費產報測試；逐日區分估計成本與帳單。 | `a9380d7` 已上線、待正常信連續驗收及精確補審。 |
| N1 新聞證據與閱讀 | 使用者 9/20–9/23 信件指出靜態公司頁、證據語氣、新聞公司標題與長信可讀性問題。 | `a9380d7` 已含部分修復，但不可用提示詞或 CI 代替正常信內容證據。 | 同一歷史素材比對特化與備援的標題歸因、數字來源時點及因果語氣；離線渲染窄螢幕，保留字體、中職、公允價值與完整資金輪動表。 | 待逐日正常信驗收。 |
| N2 章節標號 | 9/23 封存 HTML 把「七之五、情境推演與後續觀察」排在「十之二、重大政策深度解析」之後；週一模板另保留「七之五、近期預測檢討」，讀者可能看到同號不同段。 | 讀者投影把後置情境段硬編為七之五，與實際版面順序及週一專用編號不符；只影響呈現，不是分析或 state 錯誤。 | 後置情境段改為不編號的「情境推演與後續觀察」，保留內容與位置；離線合成週一兩段同時存在的案例，確認只有週報段使用七之五，且政策、情境與立場順序不變。 | 已修未發佈；139 項相關測試通過，待完整回歸、獨立審查、候選／正式 CI 與正常信驗收。 |
| R1 選股研究 | `backtest_data/prospective_selection.py` 已凍結 9/21 起的前瞻研究協議，主要持有期 20 個快照交易日，診斷持有期 5 個；費率單邊 15 bps、賣出稅 30 bps、滑價單邊 10／25 bps。以 9/23 為截止日的唯讀重算載入 258 筆歷史快照，四個組合的前瞻 cohort 均為 0。正式公式保持不變。 | 9/21 起尚不足 5 或 20 個完整快照期；快照不是已驗證交易日曆，公司行動、下市／停牌、可成交價、同開盤時點基準與一致模型版本仍是阻礙，既有歷史分組不算未碰觸樣本外。 | 重現方式：`python backtest_data/prospective_selection.py --as-of 2026-09-23`（只讀既有 state，驗證凍結的實作 SHA；輸出 `as_of_date`、`NO_REPLACEMENT` 與四組結果）。資料足夠且來源與可交易性核對後才報成本後表現、換手、風險及不確定性，不宣稱勝出或改正式係數。 | 前瞻研究持續累積；目前證據不足以替換模型。 |

E1 補充量測（唯讀分析 `state/emails/2026-09-23.html.gz`，不是重新產報）：最終 HTML 119,371 bytes；移除 `head/style/script` 後的文字約 48,400 bytes。859 個 inline `style` 的屬性值合計 30,813 bytes（加上每個 `style=` 包裝即約 37.7 KB），66 個 `href` 的位址值合計 8,881 bytes。既有壓縮器確有作用：輸出含 511 個壓縮類別標籤及 41 條 CSS 規則，不是漏接最後寄送路徑。體育區 18,969 bytes／文字 4,059 bytes，Top 3 區 12,446／3,918，其他類股 13,568／8,772，科技 8,741／5,450。119 個較長段落與 157 個較長句子均無完全相同的重複項；不能用精確字串去重冒充語義編修。下一步應用相同既有內容比較表格標記與樣式的安全精簡，以及跨段語義重述；在保留手機字體、必要連結、完整表格與單封內容前提下重新量測，未達 95 KiB 須維持風險標記。

同一封存 HTML 的起始標籤（不含結束標籤）約佔 59.2 KB，其中 249 個 `td` 約 15.8 KB、202 個 `div` 約 10.8 KB、66 個 `a` 約 10.9 KB（含不可隨意裁掉的網址），149 個 `span` 約 6.3 KB、97 個 `p` 約 5.4 KB。距 95 KiB 安全目標仍差 22,091 bytes，不能期待本輪短樣式抽取的約 1.6 KB 離線增益獨力解決。後續應以封存信逐區塊評估標籤與樣式的等價簡化，並保留跨裝置、連結及內容完整性檢查；這些標籤體積不是可直接全數刪除的浪費量。

E1 後續離線試算（仍非正式信驗收）：目前待發佈的 `compact_inline_styles` 按實際 UTF-8 損益決定是否抽取，並將字級、對齊、字色及隱藏文字保留為 inline 回退；同一樣式同時定義字色與背景時整組保留，避免收件端移除 `<style>` 後失去對比。對**已經壓縮過一次**的 9/23 封存 HTML 再套用，119,371→117,729 bytes，節省 1,642 bytes，既有可見文字、來源連結與表格數不變。這是第二次處理舊 HTML 的估計，不能當作未來正式信一次處理的節省量，更不能宣稱已低於 Gmail 約 102 KiB 門檻。該信 64 個可點來源連結共 8,881 bytes，沒有常見 `utm_*`／`fbclid`／`gclid` 參數可安全移除；其中 34 個是 Google News 連結，不可憑字串裁切冒險破壞來源。另有 432 個未帶類別且含 inline 樣式的標籤：出現至少四次的非關鍵樣式宣告原始總量約 11.6 KB，尚未扣除新增類別與 CSS 規則成本；再做通用宣告抽取也不足以填平距 95 KiB 的缺口。前三重點只有三句摘要，與後段詳解談同一事件是先結論後證據的閱讀層次，刪除收益小且會降低掃讀性。後續須針對高標記密度的表格／卡片做等價版面試驗，或逐項確認真正冗餘的敘述；不可用縮字、刪完整輪動／中職／公允價表格、斷開來源或拆信湊達標。

較早候選的離線瀏覽器核對：Chromium 關閉網路後，以 390×844 與 1440×844 兩種視窗比較封存 HTML 與當時壓縮輸出；兩版均無水平溢出，均有 66 個連結、16 個表格及 1,100 個 body 元素。逐元素比對字級、顏色、對齊、行距、顯示／隱藏、間距、邊框、背景與換行等計算樣式，兩種視窗皆無差異；手機寬度截圖中的中職賽程與上下半季表格完整可讀。之後為保護收件端移除 `<style>` 時的文字對比又改動壓縮器；**最終候選**另以 Playwright 明確設為 390×844 viewport，重查 9/20 週日與 9/24 平日封存信：原信及離線壓縮後的手機截圖各自逐位元組相同、`body.scrollWidth=390`、無元素越過視窗。這是對已封存內容再壓縮的瀏覽器相容性檢查，不等於正式信一次渲染後的縮減量，也不代表 iPhone Gmail 或正常寄信已通過。

再以 9/17–9/23 七封已封存信逐封離線套用目前待發佈的壓縮器，七封的可見文字、連結目標與表格數均未改變，也沒有一封變大；相對封存檔的試算差值依序為 1,944、1,696、1,652、4,152、1,561、1,854、1,642 bytes。這仍不是正式產報一次處理的節省量；試算後五封仍高於約 102 KiB，故信件截斷風險沒有結案。

特別是 9/20 週日綜合信當時走 `render_weekend_digest_html`，尚未呼叫本輪 `compact_inline_styles`；其 4,152 bytes 是以舊信離線套用新程式的差值，非部署後實際縮減。本輪已接上週日最終渲染路徑；其餘平日封存信已經過原有壓縮器，重套結果也只能作相容性與上界參考。9/24 另一次唯讀試算為 107,236→105,440 bytes，仍高於截斷風險線，且該信由更早的正式 SHA 產出。

N1 補充案例：9/23 正式信的甲骨文／Podcast 對照段落出現連續「。。」，原因是模型欄位已有句號、渲染器又無條件補句號；已改為只在沒有句末標點時補上並加離線回歸。同段以裁員與節目觀點推進到「資料中心擴張開始遇到現金流約束」，但裁員本身不足以證明融資因果；這仍是待核對的語義問題，不能把標點修正當成證據品質已驗收。

A1 輸入體積基準（唯讀 `state/run_manifest.json`，正式執行 2026-09-23 07:34、執行 SHA `edb7ab8bf612bc08ce85eede578f0c9f5b5b42bb`）：原始證據封包 3,006,909 字元，`market.HISTORY` 1,390,187 與 `market.STRUCTURED_NEWS_EVENTS` 587,419 字元先被裁減，100 檔 `price_forecast` 管線欄位再省 152,000 字元；留下的封包 877,495 字元、實際完整請求約 1,009,019 字元。留存區塊中 `tw_universe` 382,422、`news` 174,594、`news_clusters` 114,338 字元，顯示成本瓶頸不只是歷史資料。這些是字元量而非供應商 token 或帳單；該班執行 SHA 早於當日晚間上線的 `a9380d7` 修補，不可把此體積直接當成現行版本的表現。後續須用正常排程 manifest 比較，不能為取新數字手動呼叫付費模型。

A1 9/24 正常排程新增證據（唯讀正式 state，run `35934297029`、執行 SHA `75d5f1544e6b65fb1edce4607c7f51f969ba41bf`）：主分析請求 974,934 字元／358,298 input tokens，輸出達 70,000 tokens 且 `finish_reason=incomplete`、拒絕原因 `max_output_tokens`，最終 `analysis_origin=legacy_fallback_after_luna_failure`；07:54 寄信成功不等於特化成功。原始封包 3,029,527 字元，當班裁掉 `HISTORY` 1,408,192 與 `STRUCTURED_NEWS_EVENTS` 568,724 字元，餘下 900,803 字元；留下的 `tw_universe` 385,221、`news` 175,622、`news_clusters` 114,584 字元仍很大。估計總費用 USD 0.218976 僅涵蓋有 usage 的五次呼叫，另一次 extractor 連線失敗已送出但無 usage，不能當成帳單總額。待發佈的歷史投影只會恢復受限跨日脈絡，**尚無證據會降低最終輸出量或解決 70K 輸出耗盡**；下一步應量測完整輸出契約和仍保留的重複封包欄位，不提高 token 上限、重試次數或放寬驗證。當班 SHA 早於後續收據修正及目前候選，不能拿它判斷那些未上線修正的實際效果。

E1 9/24 正常信新增基準：最終 HTML 107,236 bytes，`gmail_clipping_possible=true`。區段中體育約 20,460、Top 3 約 12,621、Podcast 約 6,973、醫學文獻約 7,141 bytes；這些均含必要資訊，不能把整段當冗餘。待發佈壓縮器套在已壓縮的封存信約節省 1,796 bytes，仍高於約 102 KiB 風險線。對 9/20 與 9/24 封存信另以 Playwright 的 390／1440px viewport 比對壓縮前後：內文、連結、表格列數相同，592／1115 個 body 節點的計算樣式均無差異；此為離線相容性證據，非 iPhone Gmail 或未來正常信驗收。

驗收規則：修正後連續 7 次正常排程信（含週日），逐次記錄寄送、特化完整性、跨日／Podcast、觀察點、重要數字來源、最終 HTML 大小、手機閱讀與估計成本。付費 dry-run、手動重跑及舊版成功不計入。缺少原信或收件端證據時標為「待驗」，不記通過。

E1 後續修正（9/24 夜間，仍未發佈）：獨立精確 commit 審查指出，前述對已壓縮封存 HTML **再次**套用樣式器得到的節省量，無法代表正式新信只壓縮一次的效果。主代理確認後，離線還原 9/23／9/24 封存信中的 511／480 個生成類別，再以相同的近似原始 HTML 比較舊與新流程：舊樣式器分別輸出 119,371／107,236 bytes，修正後的樣式器加保守語法壓縮輸出 119,110／106,974 bytes。另以完全未帶生成類別的新鮮分析標記案例檢查，舊流程 13,585 bytes，新流程 13,549 bytes；此比較已納入離線回歸測試。保留必要字級、數字對齊、淺色標題背景與白字彩底公允價值標籤的無樣式表回退；漸層標題的純色備援僅設 `background-color`，不覆蓋正常樣式表中的漸層。還原輸入並非實際重新產報，新信變化仍待正常排程驗收；9/23 大信即使這樣處理仍高於 Gmail 約 102 KiB 風險線及 95 KiB 安全目標，不能宣稱截斷問題已解決。

R1 資料版本：上述 9/23 截止的 `observed_sha256` 為 `2bf55c888ee7e1270fa141d0270971d9ec30ecfb474df892d40bf63bfbca295f`。未來若正式 state 回補使同一截止日的雜湊不同，應視為另一資料版本，不把結果混作同一組前瞻證據。
