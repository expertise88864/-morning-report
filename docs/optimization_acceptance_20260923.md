# 晨報優化驗收清單（2026-09-23 基準）

本表只記錄可核對的工程與正式排程證據；不含持倉、個人配置或信件正文。`a9380d7f9712c622d479f6f8f87e70e2a5f00ed0` 已正式發佈，但其精確 commit 審查仍待 Claude 額度恢復補審。本表的未發佈變更不能算作已改善讀者收到的信。

| ID | 基準證據與讀者影響 | 根因／待確認 | 修正與離線驗證 | 發佈 SHA／正常信驗收 |
| --- | --- | --- | --- | --- |
| E1 信件完整性 | 9/23 `state/run_manifest.json` 的最終 HTML 119,371 bytes；`gmail_clipping_possible=true`，內容稽核未報遺失段落。9/17–9/23 七封封存信有五封超過約 102 KiB，Gmail 收件端仍可能截斷。 | `EMAIL_OVERFLOW_MODE=full` 保留全部內容，但完成渲染後只有診斷旗標，品質告警沒有消費它。9/23 HTML 的 inline style 屬性值約 37,685 bytes、連結 href 值約 9,343 bytes；不能只靠刪空白達到 95 KiB。 | 移除手機表格中無作用的重複 `mail-cell` 類別；超過 102 KiB 時明確標記風險。舊版離線樣本從 69,691 降為 68,731 bytes，連結與行列回歸通過；但 9/23 實際信沒有 `mail-cell`，不能把該樣本的縮減算成正式信改善。**仍未壓至 95 KiB**；須在不刪必要內容、連結或手機樣式的前提下，另驗證主要體積來源及收件端效果。 | 待候選與正式 CI；待正常信與收件端檢查。 |
| W1 到期觀察點 | 9/23 manifest 記錄 `watch_expired_unreviewed=1`；9/22 state 的 `w49` 建立於 9/21、期限 9/22、`last_reviewed` 空白。讀者少一次期限內回顧。 | 唯讀重放 9/22 state：開啟 7 條、容量 8 條，`usable_watch(..., '2026-09-22')` 的 7 個結果包含 `w49`，故非排序／容量漏選。9/22 manifest 為 `legacy_fallback_after_luna_failure`，備援信未完成該條驗證回顧；9/23 已過期限，不能事後改寫或假稱已回顧。 | 備援當天即揭露到期觀察點未完成驗證，於信中用自然語句提醒，品質告警附非敏感 ID／期限；不改帳本狀態、不回填。這讓缺口不再延後一天才浮出，**仍未使備援具備真正的逐項證據回顧**。 | 改善待候選與正式 CI；待正常信驗收，不得回填正式 state。 |
| A1 特化與成本 | 9/23 正式執行記錄 `analysis_origin=luna_specialized`、兩次呼叫、估計總成本 USD 0.141116；此版本的主分析與成本尚未經 `a9380d7` 後的正常信驗收。 | 舊版 9/21–9/22 曾分別遇 402／`max_output_tokens`；不可用一次成功宣稱連續可靠。 | 以既有失敗快照測試輸入切片、修補與備援，不做付費產報測試；逐日區分估計成本與帳單。 | `a9380d7` 已上線、待正常信連續驗收及精確補審。 |
| N1 新聞證據與閱讀 | 使用者 9/20–9/23 信件指出靜態公司頁、證據語氣、新聞公司標題與長信可讀性問題。 | `a9380d7` 已含部分修復，但不可用提示詞或 CI 代替正常信內容證據。 | 同一歷史素材比對特化與備援的標題歸因、數字來源時點及因果語氣；離線渲染窄螢幕，保留字體、中職、公允價值與完整資金輪動表。 | 待逐日正常信驗收。 |
| R1 選股研究 | `backtest_data/prospective_selection.py` 已凍結 9/21 起的前瞻研究協議，主要持有期 20 個快照交易日，診斷持有期 5 個；費率單邊 15 bps、賣出稅 30 bps、滑價單邊 10／25 bps。以 9/23 為截止日的唯讀重算載入 258 筆歷史快照，四個組合的前瞻 cohort 均為 0。正式公式保持不變。 | 9/21 起尚不足 5 或 20 個完整快照期；快照不是已驗證交易日曆，公司行動、下市／停牌、可成交價、同開盤時點基準與一致模型版本仍是阻礙，既有歷史分組不算未碰觸樣本外。 | 重現方式：`python backtest_data/prospective_selection.py --as-of 2026-09-23`（只讀既有 state，驗證凍結的實作 SHA；輸出 `as_of_date`、`NO_REPLACEMENT` 與四組結果）。資料足夠且來源與可交易性核對後才報成本後表現、換手、風險及不確定性，不宣稱勝出或改正式係數。 | 前瞻研究持續累積；目前證據不足以替換模型。 |

E1 補充量測（唯讀分析 `state/emails/2026-09-23.html.gz`，不是重新產報）：最終 HTML 119,371 bytes；移除 `head/style/script` 後的文字約 48,400 bytes。859 個 inline `style` 的屬性值合計 30,813 bytes（加上每個 `style=` 包裝即約 37.7 KB），66 個 `href` 的位址值合計 8,881 bytes。既有壓縮器確有作用：輸出含 511 個壓縮類別標籤及 41 條 CSS 規則，不是漏接最後寄送路徑。體育區 18,969 bytes／文字 4,059 bytes，Top 3 區 12,446／3,918，其他類股 13,568／8,772，科技 8,741／5,450。119 個較長段落與 157 個較長句子均無完全相同的重複項；不能用精確字串去重冒充語義編修。下一步應用相同既有內容比較表格標記與樣式的安全精簡，以及跨段語義重述；在保留手機字體、必要連結、完整表格與單封內容前提下重新量測，未達 95 KiB 須維持風險標記。

N1 補充案例：9/23 正式信的甲骨文／Podcast 對照段落出現連續「。。」，原因是模型欄位已有句號、渲染器又無條件補句號；已改為只在沒有句末標點時補上並加離線回歸。同段以裁員與節目觀點推進到「資料中心擴張開始遇到現金流約束」，但裁員本身不足以證明融資因果；這仍是待核對的語義問題，不能把標點修正當成證據品質已驗收。

驗收規則：修正後連續 7 次正常排程信（含週日），逐次記錄寄送、特化完整性、跨日／Podcast、觀察點、重要數字來源、最終 HTML 大小、手機閱讀與估計成本。付費 dry-run、手動重跑及舊版成功不計入。缺少原信或收件端證據時標為「待驗」，不記通過。

R1 資料版本：上述 9/23 截止的 `observed_sha256` 為 `2bf55c888ee7e1270fa141d0270971d9ec30ecfb474df892d40bf63bfbca295f`。未來若正式 state 回補使同一截止日的雜湊不同，應視為另一資料版本，不把結果混作同一組前瞻證據。
