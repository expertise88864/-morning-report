# r3(突變測試審查):repo 先前**完全沒有 pytest 設定檔**,所以沒有 per-test
# timeout。實測把五個會打真實網路的測試導到黑洞 IP 後,光兩個測試檔就跑了
# 12 分 29 秒 > ci.yml 的 timeout-minutes: 10 → job 會被 GitHub 砍掉、CI 紅燈,
# 而且與程式碼無關。單一測試逾時遠比整個 job 被砍容易診斷。
[pytest]
testpaths = tests
addopts = --strict-markers
filterwarnings =
    default
