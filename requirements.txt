yfinance>=0.2.40
numpy>=1.26.0
pandas>=2.2.0
requests>=2.31.0
feedparser>=6.0.10
opencc-python-reimplemented>=0.1.7   # 簡體→繁體(台灣用語):延燒事件等外部中文標題正名
# 測試用（CI 與本機 pytest）；不影響晨報執行
pytest>=9.1.1
# workflow 契約測試要解析 YAML。**不可改成 importorskip** ——
# 那些測試盯的正是「CI 會不會容忍失敗」,在 CI 裡跳過等於沒有守衛
# (2026-08-09:裸 import 讓 CI 連紅四個 commit,而本機一直是綠的)。
PyYAML>=6.0
# 不再需要 markdown 套件（已改用內建 re 自製轉譯器）
# 若要切回 Claude（LLM_PROVIDER=anthropic）才需取消下行註解：
# anthropic>=0.40.0
# ⚠ production 依賴由 requirements.lock 鎖定(uv pip compile --python-version 3.11 --python-platform linux 產生);本檔為人工維護的允許範圍,只有 deps-canary 與本機開發直接安裝。更新 lock:改本檔後重跑 uv pip compile(見 lock 檔首行指令)。
# 批#43:新聞正文抽取(取代整頁去標籤;實測台媒樣板雜訊佔比達 78%)
trafilatura>=2.1.0
